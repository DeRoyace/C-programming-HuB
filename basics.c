#include <stdio.h>
// #include <conio.h>
int main()
{
    printf("Hello World!\n");
    printf("This is my first program in C\n");
    
    // getch() and getche() => used to stop the terminal from closing automatically. So, it waits until user press any key.
    
    // getch();  // reads the single character from keyboard without echoing/ displaying to the screen.
    // getche(); // reads the single character from keyboard via user and displays it to the screen.
    return 0;
}