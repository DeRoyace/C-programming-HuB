#include<stdio.h>

// Bitwise Operators :
// AND (&), OR(|), XOR(^), Right SHift(>>), Left shift(<<), Tilder(~)

int main()
{

    // // Bitwise AND (&):
    // int a=19;
    // if(a & 1)
    //     printf("ODD\n");
    // else
    //     printf("EVEN\n");

    // Bitwise OR (|):
    int a=10, b=4, c;
    c= a | b;
    printf("c = %d\n", c);

    

    return 0;
}